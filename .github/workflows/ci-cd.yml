name: CI/CD - NatureApp Frontend

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]
  workflow_dispatch:

env:
  NODE_VERSION: '20'
  REGISTRY: ghcr.io
  IMAGE_NAME: jalcon00/natureapp

jobs:
  # ============================================
  # JOB 1: BUILD Y TEST DE ANGULAR
  # ============================================
  build-and-test:
    name: 🔨 Build & Test Angular
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: 📦 Install dependencies
        run: npm ci --legacy-peer-deps

      - name: 🎨 Lint (opcional)
        run: npm run lint --if-present
        continue-on-error: true

      - name: 🏗️ Build Angular (Production)
        run: npm run build -- --configuration production

      - name: 🧪 Run tests
        run: npm test -- --watch=false --browsers=ChromeHeadless
        continue-on-error: true

      - name: 📤 Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: angular-build
          path: dist/
          retention-days: 1

  # ============================================
  # JOB 2: CONSTRUIR Y PUBLICAR IMAGEN DOCKER
  # ============================================
  build-docker:
    name: 🐳 Build & Push Docker Image
    needs: build-and-test
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🔧 Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: 🔑 Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: 📝 Extract metadata (tags, labels)
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=ref,event=branch
            type=ref,event=pr
            type=sha,prefix={{branch}}-
            type=raw,value=latest,enable={{is_default_branch}}

      - name: 🏗️ Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=registry,ref=${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:buildcache
          cache-to: type=registry,ref=${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:buildcache,mode=max

      - name: ✅ Image build success
        run: |
          echo "🎉 Docker image built and pushed successfully!"
          echo "📦 Image: ${{ steps.meta.outputs.tags }}"

  # ============================================
  # JOB 3: DEPLOY A RAILWAY
  # ============================================
  deploy:
    name: 🚀 Deploy to Railway
    needs: build-docker
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🚂 Deploy to Railway
        run: |
          echo "Desplegando a Railway..."
          npm install -g @railway/cli
          railway up --service ${{ secrets.RAILWAY_SERVICE_NAME }} --detach
        env:
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}

      - name: ✅ Deployment success
        run: |
          echo "🎉 Deployment to Railway completed!"
          echo "🌐 Frontend URL: ${{ secrets.FRONTEND_URL }}"
          echo "🔧 Backend URL: https://natureapi-production.up.railway.app"

  # ============================================
  # JOB 4: HEALTH CHECK POST-DEPLOYMENT
  # ============================================
  health-check:
    name: 🏥 Health Check
    needs: deploy
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    
    steps:
      - name: 🏥 Verificar Frontend
        run: |
          echo "Esperando 30 segundos para que el servicio inicie..."
          sleep 30
          
          FRONTEND_URL="${{ secrets.FRONTEND_URL }}"
          if [ -z "$FRONTEND_URL" ]; then
            echo "⚠️ FRONTEND_URL no configurada, omitiendo health check"
            exit 0
          fi
          
          echo "Probando $FRONTEND_URL"
          response=$(curl -s -o /dev/null -w "%{http_code}" "$FRONTEND_URL" || echo "000")
          
          if [ "$response" = "200" ]; then
            echo "✅ Frontend health check exitoso!"
          else
            echo "❌ Frontend health check falló con código: $response"
            exit 1
          fi
        continue-on-error: true

      - name: 🧪 Probar integración con Backend API
        run: |
          API_URL="https://natureapi-production.up.railway.app"
          
          echo "Probando endpoint de API: $API_URL/api/places"
          response=$(curl -s "$API_URL/api/places" || echo "error")
          
          if [[ "$response" == *"placeId"* ]] || [[ "$response" == *"id"* ]]; then
            echo "✅ Integración con Backend funcionando!"
          else
            echo "⚠️ Respuesta inesperada de Backend API"
          fi
        continue-on-error: true

  # ============================================
  # JOB 5: NOTIFICACIÓN Y RESUMEN
  # ============================================
  notify:
    name: 📢 Notificar resultado
    runs-on: ubuntu-latest
    needs: [build-and-test, build-docker, deploy, health-check]
    if: always()
    
    steps:
      - name: 📊 Resumen del despliegue
        run: |
          echo "## 🚀 Resumen del Pipeline CI/CD - Frontend" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- **Commit**: ${{ github.sha }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Branch**: ${{ github.ref_name }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Actor**: ${{ github.actor }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Build & Test**: ${{ needs.build-and-test.result }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Docker Build**: ${{ needs.build-docker.result }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Deploy**: ${{ needs.deploy.result }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Health Check**: ${{ needs.health-check.result }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "✨ **Pipeline completado!**" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "🌐 **URLs:**" >> $GITHUB_STEP_SUMMARY
          echo "- Frontend: ${{ secrets.FRONTEND_URL }}" >> $GITHUB_STEP_SUMMARY
          echo "- Backend: https://natureapi-production.up.railway.app" >> $GITHUB_STEP_SUMMARY
